# Logging
logging.level.root = INFO
logging.level.hr.fer.java.vjezbe.zadatak07 = DEBUG

# Datasource
spring.datasource.url=jdbc:h2:file:~/zadatak7;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# Disable auto generating tables
# to enable Spring auto generation with schema.sql and data.sql
spring.jpa.hibernate.ddl-auto=validate

# Allow Thymeleaf templates to be reloaded at dev time
spring.thymeleaf.cache: false
spring.thymeleaf.mode: html
